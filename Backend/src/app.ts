// This file is the entry point of the application. It initializes the server and sets up any necessary configurations.

import { startServer } from "./server";

const PORT = 3000;

startServer(PORT);
